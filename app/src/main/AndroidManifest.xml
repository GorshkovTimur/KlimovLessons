<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timmyg.klimovlessons">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".network.NetworkActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".gps.GPSActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".filemanager.FileManagerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".internalstorage.StorageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".myview.CustomViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".textomcircle.TextCirlceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".secondmetr.SecondmeterActivity" />
        <activity android:name=".sqlite.EditorActivity" />
        <activity
            android:name=".sqlite.BasicActivity"
            android:label="@string/title_activity_basic"
            android:theme="@style/NoActionBar" />
        <activity android:name=".listfragment.ListFragmentActivity" />
        <activity android:name=".alertdialog.AlertActivity" />
        <activity android:name=".fragment.FragmentSecondActivity" />
        <activity android:name=".fragment.FragmentActivity" />
        <activity android:name=".broadcasrt.TimingBroadcastActivity" />
        <activity android:name=".broadcasrt.BroadcastActivity" />
        <activity
            android:name=".googlemap.GoogleMapsActivity"
            android:label="@string/title_activity_google_maps" />
        <activity android:name=".googlemap.GeoActivity" />
        <activity android:name=".notepad.NotepadSettingsActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".notepad.NotepadActivity" />
        <activity android:name=".preference.PreferencesActivity" />
        <activity android:name=".camera.CameraActivity" />
        <activity android:name=".listview.DetailViewActivity" />
        <activity android:name=".listview.ListViewActivity" />
        <activity android:name=".locale.LocaleActivity" />
        <activity android:name=".keyactivity.KeyActivity" />
        <activity android:name=".webview.BrowserActivity" />
        <activity android:name=".Animation.AnimationActivity" />
        <activity android:name=".GraphicActivity" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <receiver
            android:name=".network.NetworkReceiver"
            android:enabled="true"
            android:exported="true"></receiver>

        <receiver android:name=".widget.DaysAppWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/days_app_widget_info" />
        </receiver>
        <receiver
            android:name=".broadcasrt.MessageReciever"
            android:enabled="true"
            android:exported="true" />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>